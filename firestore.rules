rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAuth() {
      return request.auth != null;
    }
    function isOwner(uid) {
      return isAuth() && request.auth.uid == uid;
    }

    // ── Catálogos públicos (solo lectura autenticada, escritura bloqueada) ────

    match /catalogoBancos/{codigo} {
      allow read: if isAuth();
      allow write: if false;
    }

    match /catalogoCategorias/{categoriaId} {
      allow read: if isAuth();
      allow write: if false;
    }

    match /reglasCategorizacionGlobales/{reglaId} {
      allow read: if isAuth();
      allow write: if false;
    }

    match /tasasCambio/{fecha} {
      allow read: if isAuth();
      allow write: if isAuth();
    }

    // ── Datos de usuario: el owner tiene acceso completo a toda su jerarquía ─
    // La validación de integridad de datos es responsabilidad del cliente
    // (parsers + mappers + use cases en Kotlin). Las reglas solo garantizan
    // aislamiento por uid.

    match /usuarios/{uid} {
      allow read, write: if isOwner(uid);

      match /cuentas/{docId} { allow read, write: if isOwner(uid); }
      match /tarjetas/{docId} { allow read, write: if isOwner(uid); }
      match /estadosTarjeta/{docId} { allow read, write: if isOwner(uid); }
      match /movimientosTarjeta/{docId} { allow read, write: if isOwner(uid); }
      match /transacciones/{docId} { allow read, write: if isOwner(uid); }
      match /presupuestos/{docId} { allow read, write: if isOwner(uid); }
      match /metas/{docId} { allow read, write: if isOwner(uid); }
      match /cargas/{docId} { allow read, write: if isOwner(uid); }
      match /dispositivos/{docId} { allow read, write: if isOwner(uid); }
      match /configuracion/{docId} { allow read, write: if isOwner(uid); }
      match /notificacionConfig/{docId} { allow read, write: if isOwner(uid); }
      match /reglasCategorias/{docId} { allow read, write: if isOwner(uid); }
      match /reglasSugeridas/{docId} { allow read, write: if isOwner(uid); }
      match /categoriasPersonales/{docId} { allow read, write: if isOwner(uid); }

      match /{coleccion}/{docId} {
        allow read, write: if isOwner(uid);
      }
    }
  }
}
